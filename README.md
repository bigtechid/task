# bigtech Task

Internal task / kanban boards for [bigtech](https://bigtech.id).

- URL: https://task.bigtech.id
- Stack: Docker Compose + Traefik

## Deploy

```bash
docker compose build
docker compose up -d
```

Config: `planka.env` + `docker-compose.yml`.
